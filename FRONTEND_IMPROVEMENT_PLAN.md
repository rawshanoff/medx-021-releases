# 🎨 MedX Frontend: План Улучшения (Refactoring & UX)

**Текущее состояние:**
*   Frontend работает, но архитектура "хрупкая".
*   Огромные файлы (Reception.tsx > 900 строк).
*   Логика, UI и запросы к API смешаны в одну кучу.
*   "Голое" оформление, местами выглядит как прототип.

**Цель:** Сделать frontend профессиональным, модульным и красивым.

---

## 1. Архитектурные Изменения (Refactoring)

### 1.1. Global Layout (Единый макет)
Сейчас каждая страница сама оборачивает себя в `<Layout>`. Это дублирование.
**План:**
1.  Создать `RootLayout` в `App.tsx`.
2.  Использовать `<Outlet />` для рендеринга страниц внутри Layout.
3.  Удалить импорт `Layout` из каждой страницы.
*Это обеспечит, что сайдбар и хедер будут на месте всегда, даже на новых страницах.*

### 1.2. De-monolithization (Разбиение монолитов)
Страница `Reception.tsx` делает слишком много: ищет пациентов, управляет очередью, показывает модалки, печатает чеки.
**План:** Разбить на умные компоненты:
*   `features/reception/PatientSearch.tsx` (Поиск и создание)
*   `features/reception/PatientList.tsx` (Таблица результатов)
*   `features/reception/QueueSidebar.tsx` (Правая колонка с очередью)
*   `features/reception/PaymentModal.tsx` (Логика оплаты)

### 1.3. Custom Hooks (Вынос логики)
UI не должен знать, *как* мы получаем данные. Он должен только отображать их.
**План:** Создать хуки:
*   `usePatients(searchParams)`
*   `useQueue(date)`
*   `useDoctors()`
*Это упростит компоненты и сделает их читаемыми.*

---

## 2. UI/UX Улучшения (Красота и Удобство)

### 2.1. Визуальная Иерархия
Сейчас всё "плоское". Нужно добавить глубины.
*   **Карточки**: Обернуть логические блоки в компоненты `<Card>` с легкой тенью и отступами.
*   **Заголовки**: Сделать заголовки секций (Поиск, Очередь) более явными.

### 2.2. Feedback (Обратная связь)
*   **Skeletons**: Вместо мигающего "Loading..." показывать скелетон (серые плашки) при загрузке данных.
*   **Toasts**: Мы уже используем тосты, но нужно стандартизировать сообщения об ошибках.

### 2.3. Mobile Responsiveness
Адаптировать `Layout` и таблицы для корректного отображения на планшетах (многие врачи/администраторы могут использовать планшеты).

---

## 3. План Реализации (Roadmap)

### 🧹 Шаг 1: Чистка (Cleanup)
*   [ ] Исправить `App.tsx`: внедрить глобальный `Layout`.
*   [ ] Вынести типы (`Patient`, `QueueItem`) в `src/types`.

### 🧩 Шаг 2: Рефакторинг Reception (Приоритет)
*   [ ] Создать папку `src/features/reception`.
*   [ ] Вынести `QueueSidebar` (самое простое).
*   [ ] Вынести `PatientSearch` и `PatientList`.

### 💅 Шаг 3: UI Полировка
*   [ ] Внедрить Shadcn `Card` компонент повсеместно.
*   [ ] Добавить `Skeleton` лоадеры.

## User Review
Потребуется ваше одобрение на изменение структуры папок (`src/features/...`). Это стандартная практика для средних+ проектов (Feature-Sliced Design элементы).

Готовы начать с Шага 1 (Global Layout)?
