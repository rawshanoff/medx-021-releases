# ✅ Текущий статус проекта MedX

**Дата:** 2026-02-02  
**Цель:** компактное desktop-приложение (Electron) для клиники на одном ноутбуке (1366×768), с локальной БД, ролями, Soft Delete, печатью чеков, i18n.

---

## 🎯 Текущий приоритет

**Этап 0.5 — критическое исправление Frontend UI/UX для Electron** (читабельность, отступы, контраст темной темы, размеры таблиц/кнопок/инпутов).

Ориентир: `CURSOR_AGENT_PLAN.md` → **Этап 0.5**.

---

## ✅ Что уже сделано в рамках подготовки

### Этап 0.1 — аудит проекта (мусор/временные файлы)

- Удалены локальные кэши инструментов: `.pytest_cache/`, `.ruff_cache/` (включая `backend/`).
- Игнор кэшей перенесён в корневой `.gitignore`:
  - `backend/.pytest_cache/`
  - `.ruff_cache/`, `backend/.ruff_cache/`
- Удалены из папки проекта файлы с секретами/ключами (они и так игнорировались Git’ом):
  - `.env`, `frontend/.env`
  - `license.key`
  - `license_server/private_key.pem`, `license_server/public_key.pem`
- Проверено: в репозитории **нет** закоммиченных приватных ключей (по сигнатурам `BEGIN PRIVATE KEY`), а `*.pem/*.key/.env*` не отслеживаются Git.
- CI снова зелёный (исправлен фронтенд билд/зависимости и типы `ref`).

---

## 🧭 Что делаем сейчас

### Этап 0.2 — организация документации

- Собираем список документов, которые **устарели/дублируются**, и предлагаем перенести их в `docs/archive/` (только после явного подтверждения).
- Обновляем `docs/README.md`, чтобы он вёл на актуальные “точки входа”.

---

## ⏭️ Ближайшие следующие шаги

1. Подготовить предложение по архивации документов (список + почему) и запросить подтверждение.
2. После подтверждения — перенести устаревшие документы в `docs/archive/` и обновить ссылки.
3. Перейти к **Этапу 0.5** (Frontend UI/UX), выполнять итерациями по 2–3 шага.

