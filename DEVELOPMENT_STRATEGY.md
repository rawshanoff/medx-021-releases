# 🚀 MedX: Профессиональная Стратегия Разработки (AI-Ready)

Этот документ описывает стратегию запуска MVP и подготовки проекта к эффективной совместной работе с ИИ-агентами.
**Цель:** Создать надежную систему, которую легко поддерживать, развивать и масштабировать с помощью ИИ.

---

## 1. Философия: AI-First Development

ИИ-агенты работают лучше всего, когда код предсказуем, хорошо типизирован и тестируем.
Чтобы вам (и мне) было легко работать дальше:

1.  **Контекст — это всё**: Держим файлы небольшими (< 200-300 строк). Модули должны быть изолированными.
2.  **Жесткие Контракты**:
    *   **Backend**: Строгие Pydantic схемы для всех входных/выходных данных.
    *   **Frontend**: TypeScript без `any`. ИИ отлично понимает типы, это его "глаза".
3.  **Автоматическая Верификация**: ИИ может писать код, но проверять его должен тест. **Нет тестов = ИИ работает вслепую.**

---

## 2. Инфраструктура и Инструменты (Фундамент)

Настроим среду так, чтобы ошибки ловились до того, как они попадут в репозиторий.

### 2.1. Контроль версий и GitHub
*   **Репозиторий**: Подключить GitHub (private репозиторий).
*   **Ветки**: `main` (стабильная), `dev` (разработка). ИИ пишет код в ветки `feature/...` и делает Pull Request.

### 2.2. Качество Кода (Pre-commit hooks)
Установим инструменты, которые автоматически форматируют код при коммите. Это уберет споры о стиле и поможет ИИ генерировать консистентный код.

| Область | Инструмент | Зачем |
| :--- | :--- | :--- |
| **Backend** | `ruff` | Быстрый линтер (заменяет flake8, isort). Ловит баги. |
| **Backend** | `black` | Форматирование кода. |
| **Backend** | `pytest` | Запуск тестов. |
| **Frontend** | `biom` / `eslint` | Линтинг JS/TS. |
| **Frontend** | `prettier` | Форматирование. |

### 2.3. CI/CD (GitHub Actions)
Создадим пайплайн, который запускается при каждом пуше:
1.  **Lint**: Проверка чистоты кода.
2.  **Test**: Запуск unit-тестов. Если тесты упали — слияние запрещено.
3.  **Build**: Проверка, что фронтенд вообще собирается.

---

## 3. Архитектура и Технический Долг

Перед масштабированием фич нужно укрепить фундамент.

### 3.1. Устранение критических рисков
*   **[ВЫСОКИЙ] Race Conditions в Финансах**: Исправить логику обновления баланса смены (сейчас `+=` в коде, нужно атомарное обновление в БД).
*   **[СРЕДНИЙ] Обработка ошибок**: Внедрить единый механизм `AppException`. Бэкенд должен всегда возвращать `HTTP 400/404/500`, а не `{"error": "text"}` с кодом 200.

### 3.2. Стандартизация API
*   Все эндпоинты должны иметь `response_model`. Это позволит генерировать клиентский код для фронтенда автоматически (через `openapi-ts-codegen`). Это сэкономит часы ручной синхронизации типов.

---

## 4. План Запуска MVP (Roadmap)

Разбиваем работу на этапы, чтобы видеть прогресс.

### 🏁 Этап 1: Стабилизация (Текущая неделя)
*   [ ] Настройка Git и `.gitignore`.
*   [ ] Настройка линтеров (Ruff, Prettier).
*   [ ] **КРИТИЧНО**: Написание первых тестов для модуля **Finance** (открытие/закрытие смены, оплата).
*   [ ] Исправление бага с конкурентностью в кассе.

### 🚧 Этап 2: Финализация Фич (Недели 2-3)
*   [ ] **Врачи**: Завершить редактирование/удаление (сейчас есть баги).
*   [ ] **Быстрый поиск**: Реализовать поиск пациентов "на лету" (Debounce поиск на фронте + оптимизированный запрос на бэке).
*   [ ] **Интерфейс Регистратора**: Модал добавления пациента прямо из записи на прием.

### 🚀 Этап 3: Полировка и Деплой (Неделя 4)
*   [ ] **UI/UX**: Добавить "Тосты" (уведомления об успехе/ошибке).
*   [ ] **Логирование**: Настроить запись логов в файл (для разбора полетов).
*   [ ] **Docker**: Написать `docker-compose.yml` для запуска одной командой (БД + Бэк + Фронт).

---

## 5. Что делать прямо сейчас? (Next Steps)

1.  **Разрешите мне создать базовые тесты** для модуля Финансов. Это самое уязвимое место.
2.  **Подтвердите использование Github** (есть ли у вас аккаунт и репозиторий?).
3.  **Исправим баг с деньгами** (через атомарные запросы).

Жду вашей команды на страт этапа 1!
